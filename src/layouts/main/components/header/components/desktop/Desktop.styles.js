import { styled } from '@mui/system';
import { Button, IconButton } from '@mui/material';

export const GoToButton = styled(Button)(() => ({
    marginLeft: 5,
    marginRight: 5
}));

export const ButtonAvatar = styled(IconButton)(() => ({
    padding: 0,
    marginLeft: 5,
}));
